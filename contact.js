document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('contact-form');if(!form)return;form.addEventListener('submit',async e=>{e.preventDefault();const name=document.getElementById('contact-name')?.value.trim(),email=document.getElementById('contact-email')?.value.trim(),topic=document.getElementById('contact-topic')?.value,message=document.getElementById('contact-message')?.value.trim();if(!name||!email||!message){form.reportValidity?.();return}try{await apiFetch('/contact',{method:'POST',body:JSON.stringify({name,email,topic,message})});document.getElementById('form-success')?.classList.add('visible');form.reset()}catch(err){alert(err.message)}})});
